# Prevent build failures on Hostinger/Cloud deployment due to permission restrictions on esbuild postinstall binary checks
only-built-dependencies[]=@prisma/client
only-built-dependencies[]=prisma
only-built-dependencies[]=sharp
